# Copiar a .env.local (gitignored) sólo si hace falta pisar la
# detección automática de la API (ver src/api/client.ts).
# Sin esta variable, la app deriva la IP sola desde Metro (misma PC
# que corre `npx expo start`, puerto :5050).
EXPO_PUBLIC_API_BASE_URL=http://192.168.0.69:5050
